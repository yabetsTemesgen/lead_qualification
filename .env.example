# n8n webhook URL (Production URL from Webhook node after activating workflow)
N8N_WEBHOOK_URL=

# Conversational chat (OpenAI-compatible API)
OPENAI_API_KEY=
# Optional — use https://api.minimax.io/v1 for MiniMax with an OpenAI-format key
OPENAI_BASE_URL=
# Optional — default: gpt-4o-mini (use MiniMax-M2.5 etc. when using MiniMax base URL)
OPENAI_MODEL=
